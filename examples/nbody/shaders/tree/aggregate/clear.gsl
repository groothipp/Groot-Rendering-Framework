writeonly buffer { uint _VisitCount[]; };

push {
  uint _ParticleCount;
};

thread_group [256, 1, 1];

void main() {
  uint i = gl_GlobalInvocationID.x;
  if (_ParticleCount < 2u || i >= _ParticleCount - 1u) return;
  _VisitCount[i] = 0u;
}
