thread_group [64, 1, 1];

readonly buffer {
  uint values[];
} Src;

writeonly buffer {
  uint values[];
} Dst;

push {
  uint count;
};

void main() {
  uint i = gl_GlobalInvocationID.x;
  if (i >= count) return;
  Dst.values[i] = Src.values[i];
}
