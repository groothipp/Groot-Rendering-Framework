thread_group [64, 1, 1];

readonly buffer Src {
  uint values[];
} _Src;

writeonly buffer Dst {
  uint values[];
} _Dst;

push {
  uint count;
};

void main() {
  uint i = gl_GlobalInvocationID.x;
  if (i >= count) return;
  _Dst.values[i] = _Src.values[i];
}
